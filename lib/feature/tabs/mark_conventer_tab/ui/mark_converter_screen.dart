import 'package:flutter/material.dart';

class MarkConverterScreen extends StatelessWidget {
  const MarkConverterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
